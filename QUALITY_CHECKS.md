# Проверки качества Этапа 1

## 📋 Требования из документации

Согласно `Documentation/main.md`, для завершения Этапа 1 нужно проверить:

1. ✅ Нет кликов при крутилках (сглаживание 5--30 мс) - **РЕАЛИЗОВАНО**
2. ⏳ Моно-совместимость ок (корреляция ≥ 0.6) - **НУЖНО ПРОВЕРИТЬ**
3. ⏳ LUFS dry≈wet (±1 дБ) при Mix=50% - **НУЖНО ПРОВЕРИТЬ**

---

## 1. Проверка моно-совместимости

### Что проверяем:
Корреляция между левым и правым каналами должна быть ≥ 0.6

### Принцип проверки:

**Моно-совместимость** - это способность стерео сигнала корректно звучать при суммировании в моно. Если корреляция L/R каналов низкая (< 0.6), при суммировании могут возникать фазовые проблемы, провалы частот, искажения.

**Как работает проверка:**
- Скрипт `check_mono_compatibility.py` вычисляет корреляцию между левым и правым каналами
- Корреляция = 1.0 означает идентичные каналы (моно)
- Корреляция ≥ 0.6 считается приемлемой для стерео обработки
- Низкая корреляция может указывать на проблемы с BinauralFlow или MotionMod

### Как проверить:

#### Через офлайн-рендеринг:

1. Обработай файл через консоль:
```bash
./Build/offline_render input.wav output.wav "flow=0.5,energy=0.7,mix=0.5"
```

2. Запусти анализ:
```bash
python3 check_mono_compatibility.py output.wav
```

#### Через DAW:

1. Экспортируй обработанный аудио из DAW в WAV (стерео)
2. Запусти анализ:
```bash
python3 check_mono_compatibility.py output.wav
```

#### Ручная проверка в DAW:

1. Загрузи плагин на стерео трек
2. Настрой параметры (например, Flow=50%, Energy=50%, Mix=50%)
3. Экспортируй обработанный аудио
4. В DAW суммируй L и R каналы в моно
5. Сравни с оригиналом - не должно быть сильных искажений

### Ожидаемый результат:
- Корреляция ≥ 0.6 ✅
- Звук в моно не искажается критично ✅

---

## 2. Проверка LUFS (громкость)

### Что проверяем:
LUFS dry ≈ wet (±1 дБ) при Mix=50%

### Принцип проверки:

**LUFS (Loudness Units relative to Full Scale)** - стандарт измерения воспринимаемой громкости. При Mix=50% (50% dry + 50% wet) общая громкость не должна сильно меняться по сравнению с dry версией.

**Зачем это нужно:**
- Если wet версия значительно громче/тише dry, это может указывать на проблемы с балансом обработки
- Разница > 1 дБ может создавать проблемы при автоматизации Mix параметра
- Проверка помогает убедиться, что обработка не добавляет нежелательного усиления/ослабления

**Как работает проверка:**
- Скрипт `check_lufs.py` вычисляет упрощённую оценку LUFS через RMS
- Сравнивает уровни dry и wet версий
- Разница ≤ 1 дБ считается приемлемой

### Как проверить:

#### Через офлайн-рендеринг:

1. Рендери dry версию (Mix=0%):
```bash
./Build/offline_render input.wav dry.wav "mix=0.0"
```

2. Рендери wet версию (Mix=50%):
```bash
./Build/offline_render input.wav wet.wav "mix=0.5"
```

3. Запусти анализ:
```bash
python3 check_lufs.py dry.wav wet.wav
```

#### Через DAW:

1. Экспортируй dry версию (Mix=0%) в `dry.wav`
2. Экспортируй wet версию (Mix=50%) в `wet.wav`
3. Запусти анализ:
```bash
python3 check_lufs.py dry.wav wet.wav
```

**Примечание:** Для точных измерений установи `pyloudnorm`:
```bash
pip install pyloudnorm
```

#### Вариант 2: Ручная проверка в DAW

1. Загрузи плагин на трек
2. Запиши/экспортируй с Mix=0% (dry) → `dry.wav`
3. Запиши/экспортируй с Mix=50% (wet) → `wet.wav`
4. Сравни громкость в DAW или используй анализатор LUFS
5. Разница должна быть ≤ 1 дБ

### Ожидаемый результат:
- Разница LUFS ≤ 1 дБ ✅
- Громкость не меняется критично при Mix=50% ✅

---

## 3. Проверка HarmonicGlide (PsychoCore #2)

### Что проверяем:
Правильность работы HarmonicGlide - корреляция между RMS-огибающей и микро-сдвигом гармоник

### Принцип проверки:

**HarmonicGlide** должен создавать микро-питч-шифт гармоник (±2-3 цента) в ответ на изменения громкости вокала. При росте RMS (громче) → питч сдвигается вверх, при спаде RMS (тише) → питч сдвигается вниз.

**Как работает проверка:**
- Скрипт `check_harmonic_glide.py` анализирует RMS-огибающую сигнала
- Оценивает изменения питча через autocorrelation
- Проверяет корреляцию между RMS и питч-шифтом (должна быть положительной)
- Проверяет плавность изменений (отсутствие резких скачков)
- Проверяет диапазон сдвига (должен быть ±2-3 цента)

**Зачем это нужно:**
- Подтверждает, что эффект работает как задумано
- Помогает выявить проблемы с RMS-follower или питч-шифтом
- Объективно оценивает тонкость эффекта (слишком заметный сдвиг = проблема)

### Как проверить:

#### Через офлайн-рендеринг:

1. Обработай файл с параметрами для HarmonicGlide:
```bash
./Build/offline_render input.wav processed.wav "energy=0.8,flow=0.6,mix=1.0"
```

2. Запусти анализ:
```bash
python3 check_harmonic_glide.py processed.wav
```

#### Через DAW:

1. Загрузи плагин на вокальный трек с заметными изменениями громкости
2. Настрой параметры:
   - Energy = 70-100% (чувствительность)
   - Flow = 50-100% (скорость реакции)
   - Mix = 50-100% (чтобы слышать эффект)
3. Экспортируй обработанный аудио в WAV
4. Запусти анализ:
```bash
python3 check_harmonic_glide.py processed.wav
```

### Что проверяет скрипт:

1. **RMS-огибающая** - отслеживает изменения громкости
2. **Изменения питча** - оценивает микро-сдвиг гармоник через autocorrelation
3. **Корреляция RMS → питч-шифт** - должна быть положительной (при росте RMS → питч вверх)
4. **Плавность изменений** - отсутствие резких скачков (макс 5 центов между окнами)
5. **Диапазон сдвига** - должен быть в пределах ±2-3 цента

### Ожидаемый результат:
- ✅ Положительная корреляция между RMS и питч-шифтом
- ✅ Плавные изменения (без скачков)
- ✅ Диапазон сдвига ±2-3 цента

### Если проверка не пройдена:
- Увеличь Energy до 80-100%
- Используй вокал с более заметными изменениями громкости
- Проверь, что Mix > 50%

---

## 🔧 Офлайн-рендеринг: принцип работы

### Что это такое:

**Офлайн-рендеринг** - это обработка аудио файлов через плагин без использования DAW. Позволяет:
- Тестировать плагин без открытия DAW
- Автоматизировать обработку множества файлов
- Получать воспроизводимые результаты для анализа

### Как это работает:

1. **OfflineRenderer** - минимальный JUCE-хост, который:
   - Загружает WAV файл
   - Создаёт экземпляр `PluginProcessor`
   - Устанавливает параметры плагина
   - Обрабатывает аудио блоками через `processBlock()`
   - Сохраняет результат в WAV файл

2. **Утилита `offline_render`** - консольное приложение:
   ```bash
   ./Build/offline_render input.wav output.wav "flow=0.5,energy=0.7,mix=0.5"
   ```
   - Параметры передаются в формате `key=value,key=value`
   - Output по умолчанию = 200% (2.0) для нормального уровня

3. **Скрипты анализа** - Python-утилиты для объективной оценки:
   - Анализируют уже обработанные файлы
   - Вычисляют метрики (корреляция, LUFS, питч-шифт)
   - Помогают понять, работает ли эффект как задумано

### Когда это полезно:

- **Разработка:** Быстро проверить, как плагин обрабатывает конкретный файл
- **Отладка:** Сравнить результаты до/после изменений в коде
- **Анализ:** Объективно оценить работу эффектов (особенно тонких, как HarmonicGlide)
- **Автоматизация:** Обработать множество файлов с разными параметрами

### Ограничения:

- Офлайн-рендеринг не заменяет тестирование в реальной DAW
- Некоторые эффекты могут звучать по-разному в реальном времени
- Скрипты анализа дают упрощённые метрики (не заменяют слуховую оценку)

---

## 🔧 Если проверки не пройдены

### Моно-совместимость < 0.6:
- Проверь BinauralFlow - он использует только IPD (без ITD), не должен создавать проблем
- Проверь MotionMod - панорама может снижать корреляцию
- Убедись, что при Mix=0% корреляция = 1.0

### LUFS разница > 1 дБ:
- Проверь, что Mix=50% действительно даёт 50/50 dry/wet
- Проверь параметр Output - он не должен влиять на сравнение
- Убедись, что dry версия (Mix=0%) действительно не обрабатывается

