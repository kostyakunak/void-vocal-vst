# 🤖 Правила работы с ИИ для проекта VØID Engine

Документ описывает эффективные практики взаимодействия с искусственным интеллектом при разработке аудио-плагина VØID Engine.

---

## 📋 Оглавление

- [Контекст проекта](#контекст-проекта)
- [Принципы работы](#принципы-работы)
- [Структура взаимодействия](#структура-взаимодействия)
- [Как задавать вопросы](#как-задавать-вопросы)
- [Особенности аудио-разработки](#особенности-аудио-разработки)
- [Стандарты кода](#стандарты-кода)
- [Workflow и процессы](#workflow-и-процессы)
- [Чеклист перед запросом](#чеклист-перед-запросом)

---

## 🎯 Контекст проекта

### Что такое VØID Engine

**VØID Engine** — это аудио-плагин для обработки вокала, основанный на концепции "полу-универсальной структуры":

- **6 сцен** (Iceberg, Crystals, Pluto, Opiate, Glass, Platina), каждая с уникальным поведением
- **9 универсальных параметров** (Flow, Melt, Ghost, Depth, Clarity, Gravity, Energy, Mix, Output), которые меняют смысл в зависимости от сцены
- **Психоакустические кирпичи** — алгоритмы, работающие с восприятием звука
- **Технология**: JUCE framework, C++, CMake
- **Форматы**: AU, VST3, Standalone

### Ключевые документы

**ВСЕГДА ссылайся на эти документы при работе:**

1. **`Documentation/main.md`** — полная концепция проекта (1559 строк)
   - Концепция полу-универсальной структуры
   - Описание всех 6 сцен с техническими деталями
   - Психоакустические кирпичи
   - План разработки по этапам

2. **`Documentation/CURRENT_STATUS.md`** — текущий статус проекта
3. **`Documentation/PROJECT_STRUCTURE.md`** — структура файлов
4. **`README.md`** — быстрый старт и общая информация

---

## 🧠 Принципы работы

### 1. Контекст важнее скорости

**❌ Плохо:**
```
"Добавь параметр Flow"
```

**✅ Хорошо:**
```
"Добавь параметр Flow согласно документации main.md, секция 'Универсальные параметры'.
В сцене Iceberg Flow должен управлять скоростью реверб-дрейфа (LFO 0.03-0.1 Гц).
Сейчас проект на этапе 1, уже есть Mix параметр."
```

### 2. Ссылайся на конкретные документы и секции

**Используй прямые ссылки:**
- `Documentation/main.md:385` — конкретная строка
- `README.md:Структура проекта` — секция
- `Documentation/main.md#сцена-1-iceberg` — заголовок

### 3. Предоставляй текущий контекст

**Включай:**
- Текущий этап разработки
- Какие файлы уже изменены
- Какие ошибки возникли (если есть)
- Что работает, что нет

### 4. Разбивай большие задачи на шаги

**❌ Плохо:**
```
"Реализуй все 6 сцен"
```

**✅ Хорошо:**
```
"Этап 1: Реализуй базовую структуру сцен (Iceberg как MVP)
- Создай enum SceneType с 6 сценами
- Добавь метод switchScene() в PluginProcessor
- Сначала только переключение, без обработки"
```

---

## 📐 Структура взаимодействия

### Формат запроса

```
[КОНТЕКСТ]
Ссылка на документ: main.md:385
Текущий этап: Этап 1 (MVP параметров)
Текущий файл: Source/PluginProcessor.cpp

[ЗАДАЧА]
Что нужно сделать: Добавить параметр Flow

[ТРЕБОВАНИЯ]
Из документации: В Iceberg Flow управляет LFO реверба (0.03-0.1 Гц)
Ограничения: Не менять существующий Mix параметр
```

### Структура ответа ИИ

ИИ должен:
1. ✅ Прочитать релевантные документы
2. ✅ Проверить текущий код
3. ✅ Предложить конкретное решение
4. ✅ Показать изменения в контексте существующего кода
5. ✅ Указать, что нужно проверить после изменений

---

## 💬 Как задавать вопросы

### Типы запросов

#### 1. Реализация фичи

```
"Реализуй [фича] согласно [документ:секция].
Текущий контекст: [что уже есть]
Требования: [специфические детали]"
```

#### 2. Исправление ошибки

```
"Ошибка в [файл:строка]: [текст ошибки]
Контекст: [что делал перед этим]
Логи: [если есть]"
```

#### 3. Оптимизация

```
"Оптимизируй [компонент] для [цель].
Текущая реализация: [описание]
Ограничения: [что нельзя менять]"
```

#### 4. Рефакторинг

```
"Рефакторинг [компонент] для [причина].
Текущая структура: [как сейчас]
Целевая структура: [как должно быть]"
```

### Примеры хороших запросов

**Пример 1: Реализация параметра**
```
Контекст: Этап 1, уже есть Mix параметр (0-100%)
Документ: main.md:385 "Крутилки Iceberg (поведение)"
Задача: Добавить параметр Flow для сцены Iceberg

Требования:
- Flow управляет LFO реверба (0.03-0.1 Гц)
- Диапазон: 0-100%
- При Flow=0%: LFO выключен
- При Flow=100%: LFO на максимальной скорости (0.1 Гц)

Файл: Source/PluginProcessor.cpp
Не менять существующий Mix параметр.
```

**Пример 2: Исправление ошибки**
```
Ошибка компиляции в Source/PluginProcessor.cpp:45
Ошибка: 'sceneType' was not declared in this scope

Контекст:
- Добавил enum SceneType { Iceberg, Crystals, ... }
- Пытаюсь использовать в методе processBlock()

Что уже проверил:
- enum объявлен в .h файле
- #include правильный
```

**Пример 3: Рефакторинг**
```
Контекст: В PluginProcessor.cpp много параметров (Mix, Flow, ...)
Документ: main.md "Универсальные параметры"

Задача: Рефакторинг для поддержки 9 параметров

Текущая структура:
- float mixValue = 0.0f;
- float flowValue = 0.0f;
- ...

Целевая структура:
- Структура UniversalParameters { float flow, melt, ghost, ... }
- Или массив с enum индексами

Требования:
- Сохранить обратную совместимость
- Упростить добавление новых параметров
```

---

## 🎵 Особенности аудио-разработки

### JUCE Framework

**Важные концепции:**

1. **AudioProcessor** — базовый класс для обработки
2. **AudioProcessorValueTreeState** — управление параметрами
3. **AudioBuffer<float>** — буфер аудио данных
4. **processBlock()** — основной метод обработки (вызывается в реальном времени)

### Ограничения реального времени

**⚠️ КРИТИЧНО для аудио:**

- ❌ Нет `new`/`delete` в `processBlock()`
- ❌ Нет динамической аллокации
- ❌ Нет блокирующих операций (I/O, сеть)
- ❌ Минимальные вычисления (оптимизация важна)
- ✅ Используй предвыделенные буферы
- ✅ Используй lock-free структуры данных

### Параметры плагина

**Стандарт JUCE:**
- Параметры через `AudioProcessorValueTreeState`
- Автоматизация через `*Parameter` классы
- Сохранение состояния через `getStateInformation()` / `setStateInformation()`

### Пример корректной реализации параметра

```cpp
// В PluginProcessor.h
std::atomic<float>* flowParameter = nullptr;

// В PluginProcessor.cpp (prepareToPlay)
flowParameter = valueTreeState.getRawParameterValue("flow");

// В processBlock()
float flow = flowParameter->load(); // lock-free чтение
// Использование flow...
```

---

## 📝 Стандарты кода

### Стиль кода

**Основные правила:**

1. **Именование:**
   - Классы: `PascalCase` (например, `GranularEngine`)
   - Методы: `camelCase` (например, `processAudio()`)
   - Параметры: `camelCase` (например, `flowValue`)
   - Константы: `UPPER_SNAKE_CASE` (например, `MAX_GRAIN_SIZE`)

2. **Комментарии:**
   - Объясняй **почему**, а не **что**
   - Используй комментарии для сложной логики DSP
   - Ссылайся на документацию: `// См. main.md:385 "Flow в Iceberg"`

3. **Структура:**
   - Один класс = один файл (.h/.cpp)
   - Модули в отдельных файлах: `GranularEngine.h/cpp`, `SpectralEngine.h/cpp`
   - Документация в `Documentation/`

### Пример хорошего кода

```cpp
// GranularEngine.h
/**
 * Гранулярный движок для обработки аудио.
 * 
 * Концепция: main.md "Granular Engine"
 * Сцена Iceberg: main.md:373 "DSP-ядро активных модулей"
 */
class GranularEngine
{
public:
    // Конструктор с предвыделением памяти
    GranularEngine();
    
    // Настройка для конкретной сцены
    void configureForScene(SceneType scene);
    
    // Обработка блока аудио (real-time safe)
    void processBlock(AudioBuffer<float>& buffer, float flowValue);
    
private:
    // Предвыделенные буферы (no allocation in processBlock)
    AudioBuffer<float> grainBuffer;
    
    // LFO для реверб-дрейфа (Iceberg)
    float lfoPhase = 0.0f;
    static constexpr float LFO_MIN_HZ = 0.03f;  // main.md:379
    static constexpr float LFO_MAX_HZ = 0.1f;   // main.md:379
};
```

---

## 🔄 Workflow и процессы

### Типичный цикл разработки

1. **Планирование**
   - Читай `main.md` для понимания концепции
   - Определи этап из `Documentation/STAGE_*.md`
   - Создай план шагов

2. **Запрос к ИИ**
   - Опиши контекст
   - Ссылайся на документы
   - Укажи конкретные требования

3. **Реализация**
   - ИИ предлагает код
   - Ты проверяешь и тестируешь
   - При необходимости — итерация

4. **Сборка и установка (ОБЯЗАТЕЛЬНО при каждом изменении)**
   - **КРИТИЧНО**: При ЛЮБОМ изменении кода обязательно:
     1. Обновить версию плагина:
        - В `Source/PluginEditor.cpp` (строка ~158): изменить версию в строке "VØID Engine vX.X.X"
        - В `CMakeLists.txt` (строки 40 и 46): обновить `VERSION X.X.X`
     2. Собрать проект: `bash build.sh`
     3. **Обязательно установить плагин**: `bash install.sh` (иначе Ableton будет использовать старую версию из кэша)
     4. **Сообщить в чате какая версия**: после установки написать "Версия обновлена до vX.X.X"
   - Проверка в DAW (Ableton Live) — **полностью перезапустить Ableton** после установки

5. **Тестирование**
   - Компиляция: `./build.sh`
   - Установка: `./install.sh`
   - Проверка в DAW (Ableton Live)

6. **Документирование**
   - Обновляй `CURRENT_STATUS.md`
   - Коммить с понятным сообщением

### Git Workflow

**⚠️ КРИТИЧНО: Безопасная работа с Git**

**НЕ ИСПОЛЬЗУЙТЕ `git add -A`!** Это может добавить файлы из всей директории Desktop, включая Downloads и другие проекты.

**✅ Правильный способ добавления файлов:**

1. **Используйте безопасный скрипт (рекомендуется):**
   ```bash
   ./git-safe-add.sh              # Добавляет только файлы проекта
   ./git-safe-add.sh Source/      # Добавляет конкретную папку
   ```

2. **Или добавляйте файлы явно:**
   ```bash
   git add Source/ Documentation/ CMakeLists.txt
   git add .gitignore build.sh install.sh
   ```

3. **Проверяйте перед коммитом:**
   ```bash
   git status --short  # Проверьте что добавлено
   ```

**Коммиты:**
```
feat: Add Flow parameter for Iceberg scene
docs: Update CURRENT_STATUS.md with Flow implementation
fix: Correct LFO frequency calculation in GranularEngine
refactor: Extract UniversalParameters struct
```

**Ветки (если нужны):**
- `main` — стабильная версия
- `feature/flow-parameter` — новая фича
- `fix/lfo-calculation` — исправление бага

**Защита от больших файлов:**
- Pre-commit hook автоматически проверяет размер файлов (максимум 10MB)
- Файлы больше 50MB требуют подтверждения
- Медиа-файлы и архивы автоматически игнорируются (.gitignore)

---

## ✅ Чеклист перед запросом

Перед запросом к ИИ проверь:

- [ ] Прочитал релевантную документацию (`main.md`, `CURRENT_STATUS.md`)
- [ ] Знаю текущий этап разработки
- [ ] Понимаю, что уже реализовано
- [ ] Определил конкретную задачу (не общую)
- [ ] Подготовил контекст (файлы, ошибки, логи)
- [ ] Сформулировал требования и ограничения

## 🔄 Обязательный процесс после каждого изменения

**⚠️ КРИТИЧНО: При ЛЮБОМ изменении кода выполнять следующие шаги:**

### 1. Обновление версии

**Перед сборкой всегда обновляй версию:**

1. **В `Source/PluginEditor.cpp`** (строка ~158):
   ```cpp
   auto versionText = UTF8_STRING("VØID Engine vX.X.X - Build ") + ...
   ```
   Увеличь версию (например: v1.0.4 → v1.0.5)

2. **В `CMakeLists.txt`** (две строки):
   - Строка 40: `project(AudioPluginDemo VERSION X.X.X)`
   - Строка 46: `VERSION X.X.X` в `juce_add_plugin`

### 2. Сборка и установка

```bash
# Всегда выполняй оба шага:
bash build.sh    # Сборка
bash install.sh  # Установка (ОБЯЗАТЕЛЬНО!)
```

**Важно:** Без `install.sh` Ableton будет использовать старую версию из кэша!

### 3. Сообщение о версии

**После установки обязательно сообщи в чате:**
```
"Версия обновлена до vX.X.X"
```

Это помогает:
- Отслеживать, какая версия загружена в Ableton
- Понимать, что изменения применены
- Быстро находить проблемные версии

### 4. Перезапуск DAW

**После установки:**
- Полностью закрой Ableton Live
- Открой заново
- Проверь версию в GUI плагина (должна совпадать с обновленной)

---

**Почему это важно:**
- Ableton кэширует плагины, старые версии могут оставаться в памяти
- Новая версия в GUI — визуальный индикатор, что изменения применены
- Сообщение в чате создает историю изменений

### Пример подготовки

**Перед запросом:**

1. ✅ Прочитал `main.md:385` про Flow в Iceberg
2. ✅ Проверил `CURRENT_STATUS.md` — есть Mix, нет Flow
3. ✅ Посмотрел `Source/PluginProcessor.cpp` — вижу структуру Mix
4. ✅ Определил задачу: добавить Flow аналогично Mix
5. ✅ Требования: LFO 0.03-0.1 Гц, диапазон 0-100%

**Запрос:**
```
Контекст: Этап 1, уже реализован Mix параметр
Документ: main.md:385 "Крутилки Iceberg (поведение)"
Файл: Source/PluginProcessor.cpp (строка 45-60 для Mix)

Задача: Добавить параметр Flow для Iceberg

Текущая реализация Mix:
- AudioProcessorValueTreeState параметр "mix" (0-1.0)
- Использование в processBlock()

Требования для Flow:
- Параметр "flow" (0-1.0)
- Управляет LFO реверба (0.03-0.1 Гц согласно main.md:379)
- Аналогичная структура как у Mix

Не менять существующий код Mix.
```

---

## 🎯 Специфичные паттерны для VØID Engine

### 1. Работа со сценами

**Паттерн: Scene-based behavior**

```cpp
// PluginProcessor.h
enum class SceneType {
    Iceberg,
    Crystals,
    Pluto,
    Opiate,
    Glass,
    Platina
};

// В processBlock()
switch (currentScene) {
    case SceneType::Iceberg:
        // Flow управляет LFO реверба (main.md:379)
        processIcebergFlow(flowValue);
        break;
    case SceneType::Crystals:
        // Flow управляет дрожью гранул (main.md:540)
        processCrystalsFlow(flowValue);
        break;
    // ...
}
```

### 2. Универсальные параметры

**Паттерн: Universal Parameters**

```cpp
// Структура для всех 9 параметров
struct UniversalParameters {
    float flow = 0.0f;
    float melt = 0.0f;
    float ghost = 0.0f;
    float depth = 0.0f;
    float clarity = 0.0f;
    float gravity = 0.0f;
    float energy = 0.0f;
    float mix = 0.0f;
    float output = 2.0f;  // Output=200% даёт нормальный уровень (1.0x), Output=100% даёт половину громкости (0.5x)
};

// В каждой сцене параметры имеют разный смысл
// См. main.md "Поведение этих параметров в разных сценах"
```

### 3. Психоакустические кирпичи

**Паттерн: Psychoacoustic Bricks**

```cpp
// Базовый класс для кирпичей
class PsychoacousticBrick {
public:
    virtual void process(AudioBuffer<float>& buffer) = 0;
    virtual void configure(const JsonValue& config) = 0;
};

// Конкретные реализации
class BinauralFlow : public PsychoacousticBrick {
    // main.md:440 "Iceberg v2 --- с кирпичом Binaural Flow"
};

class HarmonicGlide : public PsychoacousticBrick {
    // main.md:956 "Harmonic Glide"
};
```

---

## 🚨 Типичные ошибки и как их избежать

### Ошибка 1: Недостаточный контекст

**❌ Плохо:**
```
"Добавь параметр"
```

**✅ Хорошо:**
```
"Добавь параметр Flow для сцены Iceberg согласно main.md:385.
Текущая реализация Mix в PluginProcessor.cpp:45-60.
Требования: LFO 0.03-0.1 Гц, диапазон 0-100%."
```

### Ошибка 2: Игнорирование документации

**❌ Плохо:**
```
"Сделай реверб"
```

**✅ Хорошо:**
```
"Реализуй реверб для Iceberg согласно main.md:379:
- Сверхдлинный decay (до 20 сек)
- Движение по панораме с LFO (0.05-0.1 Гц)
- Предзадержка 80-120 мс"
```

### Ошибка 3: Нарушение real-time ограничений

**❌ Плохо:**
```cpp
void processBlock(...) {
    auto* buffer = new float[1024]; // ❌ Аллокация в RT!
}
```

**✅ Хорошо:**
```cpp
class Processor {
    AudioBuffer<float> preallocatedBuffer; // ✅ Предвыделено
    
    void processBlock(...) {
        // Используем preallocatedBuffer
    }
};
```

---

## 📚 Дополнительные ресурсы

### JUCE Документация
- [JUCE Tutorial](https://juce.com/learn/tutorials)
- [AudioProcessorValueTreeState](https://docs.juce.com/master/classAudioProcessorValueTreeState.html)
- [Real-time Audio Programming](https://juce.com/learn/tutorials/creating-an-audio-plugin)

### Внутренние документы
- `Documentation/main.md` — полная концепция (1559 строк)
- `Documentation/SOUND_CONTRACTS.md` — звуковые контракты и метрики качества
- `Documentation/QUICK_START.md` — быстрый старт
- `Documentation/PROJECT_STRUCTURE.md` — структура проекта
- `README.md` — общая информация

---

## 🔄 Обновление этого документа

Этот документ должен обновляться по мере развития проекта:

- Добавляй новые паттерны
- Фиксируй типичные ошибки
- Обновляй примеры на основе реального опыта
- Ссылайся на новые документы

---

**Последнее обновление:** 2025-01-XX  
**Версия:** 1.0  
**Проект:** VØID Engine

