# Текущий статус проекта

## ✅ Этап 0: Инфра-старт - ЗАВЕРШЕН

**AudioPluginDemo работает корректно!**

### Что установлено:
- ✅ AudioPluginDemo.vst3 (VST3 формат)
- ✅ AudioPluginDemo.component (AU формат)

### ✅ Реализовано:

**Параметры (все 9 параметров):**
- ✅ Flow (0.0-1.0, default: 0.0)
- ✅ Melt (0.0-1.0, default: 0.0)
- ✅ Ghost (0.0-1.0, default: 0.0)
- ✅ Depth (0.0-1.0, default: 0.0)
- ✅ Clarity (-0.5-0.5, default: 0.0)
- ✅ Gravity (0.0-1.0, default: 0.0)
- ✅ Energy (0.0-1.0, default: 0.0)
- ✅ Mix (0.0-1.0, default: 0.0)
- ✅ Output (0.0-2.0, default: 2.0) - **Важно:** Output=200% даёт нормальный уровень (1.0x), Output=100% даёт половину громкости (0.5x)

**Технические детали:**
- ✅ Сглаживание параметров (30ms) для предотвращения кликов
- ✅ GUI с 9 слайдерами в сетке 3x3
- ✅ Сохранение/загрузка состояния всех параметров
- ✅ При Mix=0% звук проходит без изменений (bit-perfect dry) при Output=200%
- ✅ Латентность = 0

### ✅ Заглушки модулей (Этап 0 завершен):
- ✅ GranularEngine - базовая структура
- ✅ SpectralEngine - базовая структура
- ✅ SpaceEngine - базовая структура
- ✅ DynamicLayer - базовая структура
- ✅ MotionMod - базовая структура
- ✅ Модули интегрированы в процессор (пока без обработки, pass-through)

## 🎯 Этап 1: Ядро DSP (черновое) - В ПРОЦЕССЕ

### ✅ Реализовано:

1. **SpaceEngine** - реверб (juce::dsp::Reverb) ✅
   - Pre-delay 100-200 мс (оптимизировано для мужского вокала)
   - Depth → decay time, room size, pre-delay
   - Flow → stereo width
   - Ghost → wet/dry level
   - Параметры подключены и работают

2. **MotionMod** - LFO для панорамы/громкости ✅
   - Flow → частота LFO (0.03-0.08 Гц, медленнее для мужского вокала)
   - Energy → амплитуда модуляции (pan ±8%, gain ±3%)
   - Gain LFO со сдвигом фазы на 90° (противофазно с pan)
   - Параметры подключены и работают

3. **GUI улучшения** ✅
   - Визуальная индикация заглушек: Melt, Clarity, Gravity помечены "(stub)" и затемнены
   - Размер окна по умолчанию исправлен для корректного отображения всех 9 слайдеров

### ✅ Реализовано (продолжение):

4. **PsychoCore #1: Binaural Flow** (Iceberg) ✅
   - Фазовая модуляция (IPD) между L/R каналами через упрощенные all-pass фильтры
   - Медленный LFO (0.03-0.08 Гц) модулирует фазовый сдвиг (5-10 градусов)
   - Дополнительная фазовая модуляция на верхах (5-12 кГц) при Ghost > 0
   - **КРИТИЧНО:** НЕ используются задержки времени (ITD) - они создают панораму
   - Эффект работает БЕЗ панорамы - создаёт «дыхание пространства»
   - Параметры: Flow (скорость LFO), Depth (амплитуда фазового сдвига), Ghost (фазовая модуляция на верхах)
   - Интегрирован в цепь после Granular, перед Reverb

5. **PsychoCore #2: Harmonic Glide** (Platina) ✅
   - RMS-follower с медленным релизом (450 мс) для отслеживания энергии вокала
   - Микро-питч-шифт гармоник (±2-3 цента) в ответ на изменение RMS
   - При росте RMS → гармоники сдвигаются вверх
   - При спаде RMS → гармоники сдвигаются вниз
   - Плавное сглаживание всех параметров для отсутствия скачков
   - Параметры: Energy (чувствительность к громкости), Flow (скорость реакции)
   - Интегрирован в цепь после BinauralFlow, перед Space

6. **Исправление Output параметра** ✅
   - Output по умолчанию изменён с 1.0 (100%) на 2.0 (200%)
   - Output=200% даёт нормальный уровень (1.0x) - это правильное значение по умолчанию
   - Output=100% даёт половину громкости (0.5x)
   - Output=50% даёт четверть громкости (0.25x)
   - Исправлена нормализация параметра в OfflineRenderer (setValueNotifyingHost ожидает 0.0-1.0)
   - GUI показывает Output в процентах (0-200%)
   - При Mix=0% и Output=200% получается bit-perfect dry сигнал

7. **Офлайн-рендеринг и инструменты тестирования** ✅
   - Реализован `OfflineRenderer` - консольный хост для плагина (позволяет обрабатывать WAV файлы без DAW)
   - Утилита `offline_render` для обработки аудио через командную строку
   - Вспомогательные Python-скрипты для анализа результатов:
     - `check_mono_compatibility.py` - анализ корреляции L/R каналов
     - `check_lufs.py` - сравнение уровней громкости
     - `check_harmonic_glide.py` - проверка корреляции RMS и питч-шифта
   - Скрипт `render_and_test.sh` - пример автоматизации процесса (рендеринг + проверки)
   - **Принцип:** Офлайн-рендеринг позволяет тестировать плагин без DAW, скрипты помогают объективно оценить результаты обработки

### ⏳ Осталось реализовать (Этап 1):

3. **SpectralEngine** - air-shelf/tilt, лёгкий exciter (частично реализован - EQ работает, формант-шифт отключен)

### 📋 Проверки Этапа 1 (из main.md):

- ✅ Нет кликов при крутилках (сглаживание 30 мс) - реализовано
- ⏳ Моно-совместимость ок (корреляция ≥ 0.6) - нужно проверить
- ⏳ LUFS dry≈wet (±1 дБ) при Mix=50% - нужно проверить

## 📌 Важно

- Используем только AudioPluginDemo
- Все параметры добавлены и работают
- Сглаживание предотвращает клики при изменении параметров

